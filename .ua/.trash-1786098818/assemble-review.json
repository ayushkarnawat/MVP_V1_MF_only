{
  "fixedSectionOk": true,
  "nodesRecovered": 0,
  "edgesRestored": 0,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "Merge script's 'Fixed' section (11 tested_by edges dropped as orphan/test-test/prod-prod pairs) is proportionate at ~1% of 1132 input edges — no systemic issue.",
    "No node ID normalization occurred at all: sum of nodes across the 23 logical batch outputs is exactly 505, matching both input and output node counts in the merge report, with zero ID collisions. Batch outputs were already clean.",
    "Verified directly (not just trusting the report): 0 nodes missing an id, 0 unknown node types (file/function/class/document/config/resource/pipeline/service all present and consistent with the report), 0 unknown complexity values, 0 dangling edges, 0 duplicate (source,target,type) edge keys, 0 nodes missing summary/complexity/filePath, 0 edges missing weight, 0 imports edges missing direction.",
    "resource-type nodes intentionally use a 'step:' ID prefix (3 nodes, CI job steps in .github/workflows/ci.yml) rather than 'resource:' — this is a distinct, consistently-applied convention, not a mismatch.",
    "Cross-batch import edges: checked all 420 (source,target) pairs from scan-result.json's importMap against the assembled graph. 419/420 already present as 'imports' edges (matches the edge-type count of 419 exactly) — the per-batch analyzers had already extracted these from neighborMap data, which is why the merge script's own importMap recovery pass found 0 to add. The 1 unmatched pair is backend/app/models/__init__.py importing itself (a self-loop artifact of the import scanner, not a real file-to-file edge) — correctly left out, not restored.",
    "No nodes recovered, no edges restored, no cross-batch edges added, no type/complexity remapping performed — the assembled graph required no corrections beyond what the merge script already did."
  ]
}
